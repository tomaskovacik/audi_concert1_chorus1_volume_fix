name: Compile Sketch

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compile sketch
        uses: arduino/compile-sketches@v1
        with:
          fqbn: "stm32duino:STM32F1:genericSTM32F103C"
          platforms: |
            - name: arduino:sam
            - name: stm32duino:STM32F1
              source-url: https://github.com/rogerclarkmelbourne/Arduino_STM32.git
              source-path: STM32F1
          libraries: |
            - name: FlexWire
          sketch-paths: |
            - SW/audi_volume_fix_stm32
